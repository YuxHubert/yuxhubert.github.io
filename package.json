{
  "name": "yuxhubert.github.io",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "node --test tests/*.test.mjs",
    "deploy:pages": "node scripts/deploy-pages.mjs",
    "deploy:pages:dry-run": "node scripts/deploy-pages.mjs --dry-run"
  },
  "dependencies": {
    "astro": "^5.0.0"
  },
  "devDependencies": {}
}
